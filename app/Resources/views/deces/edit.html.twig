{% extends "@MairieMairie//layout.html.twig" %}

{% block body %}
   <style type="text/css">
    h5{
        text-align: center;

    }
    #container{width: 500px;
    background-color: #BAC1C8;
    }
    #create{
        margin-left: 100px;
    }
    #list{
        margin-left: 100px;
    }
</style>
    <h5>Dece creation</h5>
    <div id="container" class="container">
    {{ form_start(edit_form, {'attr': { 'novalidate': 'novalidate'}}) }}
 <div class="form-group">
        {{ form_label(edit_form.nomDeces) }}
        {{ form_errors(edit_form.nomDeces) }}
        {{ form_widget(edit_form.nomDeces, {'attr': { 'class': 'form-control'}}) }}
    </div>

    <div class="form-group">
        {{ form_label(edit_form.prenomDeces) }}
        {{ form_errors(edit_form.prenomDeces) }}
        {{ form_widget(edit_form.prenomDeces, {'attr': { 'class': 'form-control'}}) }}
    </div>

      <div class="form-group">
        {{ form_label(edit_form.lieuDeces) }}
        {{ form_errors(edit_form.lieuDeces) }}
        {{ form_widget(edit_form.lieuDeces, {'attr': { 'class': 'form-control'}}) }}
    </div>
 <div class="form-group">
        {{ form_label(edit_form.dateDeces) }}
        {{ form_errors(edit_form.dateDeces) }}
        {{ form_widget(edit_form.dateDeces, {'attr': { 'class': 'form-control'}}) }}
    </div>
     <div class="form-group">
        {{ form_label(edit_form.age) }}
        {{ form_errors(edit_form.age) }}
        {{ form_widget(edit_form.age, {'attr': { 'class': 'form-control'}}) }}
    </div>
   
       
      <table>
        
        <input id="create" class="btn btn-primary" type="submit" value="Modifier" />
    {{ form_end(edit_form) }}

    
            <a id="list" class="btn btn-default" href="{{ path('deces_index') }}">retour</a>
        </table>
   
     </div>

{% endblock %}
